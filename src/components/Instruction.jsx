import React from "react";
import "../style.css";

function Instruction() {
  return (
    <div>
      <p>
        This tool lets you set a weekly budget and track expenses, starting with
        placeholder data. You can reset everything easily, input your budget,
        and submit it. Enter expenses for each day or select days and submit
        them to the tracker.
      </p>
    </div>
  );
}

export default Instruction;
